# vim: tabstop=2 expandtab shiftwidth=2 softtabstop=2 smartindent nu ft=yaml
---
exclude_paths:
  - molecule
  - .github/

skip_list:
  - name[template]
  - name[casing]
  - yaml[octal-values]
  - key-order[task]
